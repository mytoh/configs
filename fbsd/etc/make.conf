.if ${.CURDIR:M/usr/ports*}
PORTSDIR = /usr/ports
_PORTSDIR=${.CURDIR:C@^${PORTSDIR}@@}
.include "/etc/make.conf.ports"
.endif
KERNCONF=CURRENT
# pkgng
#WITH_PKGNG=devel
# added by use.perl 2013-09-21 21:53:22
PERL_VERSION=5.16.3
